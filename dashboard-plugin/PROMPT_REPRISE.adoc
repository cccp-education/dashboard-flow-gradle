= PROMPT_REPRISE — dashboard-flow-gradle
:session: 001
:date: 2026-06-23

== Mission de la session en cours

**DFL-1 — React Flow dashboard de base.**

1. Scaffold Vite + React + TypeScript + React Flow dans `ui/`.
2. Charger le `graph.json` produit par `graphify-gradle`.
3. Rendre les nœuds et arêtes de base dans React Flow.
4. Ajouter une sidebar d'information au clic sur un nœud.
5. Ajouter une recherche simple par label ou type.
6. Tests unitaires Vitest.

== Références

* `DASHBOARD_REACT_FLOW.adoc` (racine workspace)
* `AGENT.adoc`
* `.agents/INDEX.adoc`
* `BACKLOG.adoc`
